I wrote the p3helper.c code myself.

I believe everything works perfectly under all conditions. 

How successful coordination was achieved:

Semaphores:
In the initStudentStuff function if a robot successfully created the semaphore it sets the initial value 0 in order to lock it. 
All other robots then proceed to the else statement and then performs a sem_wait() which are blocked until it is released by the initializing robot.
With the non-initialized robots blocked, the initalizing robot takes its time to create the common files. Afterwards, it performs a sem_post() which the other robots can then read from the common files.
This avoids a race condition and ensures that the robots will always wait for the files to be created first before reading from it.
 
Upon entering the placeWidget function, a sem_wait is placed in order to allow only one process at a time to modify the files and print the files. Once the process has finished, 
it then performs a sem_post to allow other processes waiting to enter the semaphore to run. If it is the last process, the common file descriptors are closed and the file names are unlinked.
The   

Triangular Printing Algorithm:
In order to achieve successful printing of a triangular pattern, I had to create an additional 3 variables: rowprint, printcount, and maxpeakhit. Rowprint keeps track of the 
row size that needs to be printed. Printcount keeps track of the number of processes printed in the current line. Maxpeakhit acts as a flag which changes value if the process
past the halfway point gets printed. This helps check when the rowsize needs to start decrementing. First the process increases the printcount, which indicates which value 
it is on the row. It then checks if the count value is greater than the total amount of robots divided by 2 in order to determine whether to set the maxpeakhit flag. If the process
printcount is equal to the rowprint (e.g. it is the 3rd value printed on the 3rd row) then it is the last value for that row. Depending on the value of maxpeakhit, it will
either increment or decrement the rowsize. It will then print out its process ID and then reset the value of printcount to zero. If the process is not the final process or a 
line terminating process then it just simply prints its process ID. 

I wrote the p3helper.c code myself.

I believe everything works perfectly under all conditions.

How successful coordination was achieved:


Semaphores:
In the initStudentStuff function if a robot successfully created the semaphore it sets the initial value 0 in order to lock it.                      
All other (non-initializing) robots proceed to the else statement and then performs a sem_wait(). Since the value is 0, these robots are blocked until it is released by the initializing robot.
With the non-initialized robots blocked, the the initializing robot takes its time to create the common files. Afterwards, it performs a sem_post(). When this happens, the non-initializing robot then opens and read from the common files.
This avoids a race condition and ensures that the non-initializing robots will always wait for the files to be created first before reading from it. 

Upon entering the placeWidget function, a sem_wait() is placed in order to allow only one robot at a time to update the files and print. Since the robots have to first wait for the common files to be initialized, it is impossible to enter the 
critical region inside placeWidget before the common files have been created. Once the robot has finished printing, if it is not the terminating robot it then performs a sem_post to allow other robots waiting to enter the critical region to run. 
If it is the terminating robot, then it performs file cleanups by closing the file descriptors and unlinking the file names. This also occurs with the semaphore cleanup by using sem_close() and sem_unlink().


Triangular Printing Algorithm:
In order to achieve successful printing of a triangular pattern, I had to create  4 variables: count, rowprint, printcount, and maxpeakhit each with an initial value of 0, 1, 0, and 0 respectively. 
Count keeps track of the total number of processes printed. Rowprint keeps track of the row it is on which corresponds to the amount of robots that needs to be printed for that line. 
Printcount keeps track of the number of robots printed in the current row. Maxpeakhit acts as a flag which changes value if the process
past the halfway point gets printed. Maxpeakhit helps check when the rowsize needs to start decrementing. 

First the robot checks if it is the terminating robot. This is done by checking if the current amount of robots (count) equals to nrRobots * quota. If it is then it performs a printeger, followed by a printf("F\n") and an fflush then the file and semaphore cleanup (strategy of cleanup is described above).

If it is not the terminating robot, then printcount is increased. This indicates how many robots are on the current row so far. The robot then checks if its count value (total amount printed) is greater than the [(nrRobots * quota) / 2] in order to determine 
if more than half of the total processes have been printed and whether to set the maxpeakhit flag. 

The robot then checks if it is the last value for that row by checking if printcount is equal to the rowprint (e.g. the 3rd value printed on the 3rd row)
If it is, then it has to decide whether to increment or decrement the next row. This depends on the maxpeakhit value. If the maxpeakhit flag is set, it will start decrementing the rowprint, which shortens the length of each succeeding row 
in order to create a triangle form. The robot will then print out its process ID and then reset the value of printcount to zero. It then performs a printeger followed by a printf("N\n"), an fflush, and resetting the printcount to 0. 

If the robot is not the final process or a line terminating process then it just simply prints its process ID and then an fflush.

To summarize: each robot is first checked if it is the last robot. If it is, then it performs file and semaphore cleanup. 
If the program did not terminate then the bot checks if it has printed out more than half of its current process to determine whether to set the maxpeakhitflag. 
Then it checks if it is the last robot for that row. If it is, then depending on the maxpeakhitflag, it either increments or decrements the value of the next row. 
If it is not the last robot for that row, then it simply prints. 

